language: php
php: 7.4
services:
  - docker
before_install:
  - composer install
  - docker-compose up -d
  - php bin/console doctrine:migrations:migrate --no-interaction
script:
  - vendor/bin/phpunit